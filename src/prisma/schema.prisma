// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Posts {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  published Boolean
  title     String
  content   String
  authorId  String
  author    User     @relation(name: "PostsToUser", fields: [authorId], references: [id])

  @@map("post")
}

model User {
  id        String   @id @default(cuid())
  posts     Posts[]  @relation(name: "PostsToUser")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  email     String   @unique
  password  String
  firstname String?
  lastname  String?
  role      Role

  @@map("user")
}

enum Role {
  ADMIN
  USER
}
